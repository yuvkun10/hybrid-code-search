# The default HashingEmbedder needs none of these; search works fully offline.
# Set these only to use the optional sentence-transformers backend.
SCS_EMBEDDER=hashing
SCS_MODEL=sentence-transformers/all-MiniLM-L6-v2
